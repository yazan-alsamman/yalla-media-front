# ─── Local API (Desktop\Yalla Media) ─────────────────────────────────────────
# Run: cd "C:\Users\LENOVO\Desktop\Yalla Media" && php artisan serve
# Leave VITE_API_URL unset so the dashboard uses /api and Vite proxies to http://127.0.0.1:8000
# (see vite.config.ts → server.proxy).

# ─── Remote API (Hostinger) — uncomment when you are NOT using local Laravel ─
# VITE_API_URL=https://salmon-mouse-246850.hostingersite.com/api
VITE_API_URL=http://127.0.0.1:8000/api

# ─── Mobile app (Yalla_media_app) ────────────────────────────────────────────
# Same Laravel: use .env EXPO_PUBLIC_API_URL only when needed, or EAS eas.json env for store builds.
# Local: unset EXPO_PUBLIC_API_URL + php artisan serve --host=0.0.0.0 --port=8000